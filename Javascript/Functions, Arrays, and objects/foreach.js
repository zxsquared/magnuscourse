// var nums = [1,2,3,4,5,5,7,8,8,]

// nums.forEach(function(num){
// console.log(num);
// })

function myForEach(arr, func){
    // loop through arr
    for (var i = 0; i < arr.length; i++){
        // Call func()
        func()
    }
}

var colors = ["red", "orange", "yellow"]

myForEach(colors,)